node_modules
# Keep environment variables out of version control
.env

# generated/prisma is committed to git so Railway always has the correct Prisma client
# (prisma generate also runs during build: "prisma generate && tsc")
